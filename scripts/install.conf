# APM 설치 환경 설정 파일
# 실제 설치 환경에 맞춰 아래 값을 수정하세요.

# APM 프로그램 및 설정 파일이 저장될 경로
INSTALL_DIR="/opt/apm"

# DB 데이터 및 로그 등 대용량 데이터가 저장될 경로
DATA_DIR="/data/apm"

# OpenTelemetry Java Agent가 배포될 경로 (WAS 서버 접근 가능 경로)
AGENT_DIR="/waslib"

# APM 대시보드 및 OTLP 수신을 위한 외부 노출 포트
APM_PORT="8080"
